# LCNI Data Collector - Version 5.3.9h

## Changes
- Nâng giao diện shortcode `[lcni_member_login]`: gom label/input/button vào một khung riêng có tùy chỉnh nền, viền, bo góc; căn lề trái đồng bộ label/input.
- Bổ sung nút `Đăng ký` song song nút `Đăng nhập` theo layout 50/50 full-width trong khung login; cho phép chọn page redirect khi bấm `Đăng ký` từ phần cài đặt tab Login.
- Nâng giao diện shortcode `[lcni_member_register]`: gom các trường vào một khung riêng có tùy chỉnh nền/viền/bo góc và căn lề trái label/input.
- Mở rộng cấu hình Quote: thêm chọn font family, căn lề, hiệu ứng chữ (normal/italic/bold/uppercase/shadow) và ô preview 1 dòng theo thời gian thực trong trang settings.
- Nâng version plugin lên `5.3.9h` và bump version asset Chart Builder để cache-busting.
